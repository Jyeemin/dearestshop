spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

jwt:
  secret: dearest-shop-jwt-secret-key-2026-very-long-string

file:
  fileDir: "C:/Users/USER/Desktop/Study/images/"

logging:
  level:
    org.hibernate.SQL: debug